# Signatures and drivers

The model-facing layer: per-node force signatures and the whole-model
analysis drivers. See [Signatures and feature vectors](../guide/signatures.md)
for the walkthrough.

```@autodocs
Modules = [AsapHarmonics]
Pages = ["src/types.jl"]
```

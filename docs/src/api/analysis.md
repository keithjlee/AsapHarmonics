# Analysis layer

Distances, complexity scores, and clustering over nodal feature vectors —
including the entry points implemented in package extensions
(Clustering.jl → [`cluster_nodes`](@ref), MultivariateStats.jl →
[`embed_nodes`](@ref), AsapOptim → [`harmonic_params`](@ref) and friends).
See [Distances, complexity, clustering](../guide/analysis.md) for the
walkthrough.

```@autodocs
Modules = [AsapHarmonics]
Pages = ["src/analysis.jl"]
```

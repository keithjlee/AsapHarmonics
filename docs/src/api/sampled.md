# Sampled path and visualization

The grid/transform pipeline. Descriptors never need it — it exists for
numeric cross-validation of the closed forms and for visualizing signatures
as sampled surfaces (see
[Visualizing signatures](../guide/signatures.md#Visualizing-signatures)).

```@autodocs
Modules = [AsapHarmonics]
Pages = ["src/functions.jl"]
```
